# Java / Gradle. Both projects produce build/: the plugin at mc-plugin/build/
# and the mod at mc-plugin/neoforge/build/, which also holds the NeoForge and
# Minecraft artifacts the toolchain downloads - none of that is ever committed.
build/
.gradle/
*.class

# PHP / Composer
flarum-extension/vendor/
composer.lock

# Editor / OS
.idea/
.vscode/
*.iml
.DS_Store
Thumbs.db

# Node
node_modules/

# Runtime artefacts
*.log
