# Java / Gradle
mc-plugin/build/
mc-plugin/.gradle/
*.class

# PHP / Composer
flarum-extension/vendor/
composer.lock

# Editor / OS
.idea/
.vscode/
*.iml
.DS_Store
Thumbs.db

# Node
node_modules/

# Runtime artefacts
*.log

# Generated by the velocity-api annotation processor during a build; the one that
# ships is packed into the jar from build/classes, never from the source tree.
mc-plugin/velocity-plugin.json
