// Shared between the forum and admin frontends. Intentionally minimal: every
// string lives in ../../locale/*.yml so both languages stay in one place.
export const EXTENSION_ID = 'stalir-mc-bridge';
